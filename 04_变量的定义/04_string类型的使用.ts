let message1 = "abc"
let message2 = "hello world"

// 一般情况下，在能够自动推导出类型时，是不需要加类型注解的
const name = "xkf"
const age = 25
const height = 1.85
let message3 = `name:${name} age:${age} height:${height}`
console.log(message3)

export {}